#include <stdio.h>

/**
 * main - Entry point
 *@void: Null value
 *
 * Description: Check the last digit of the number
 * Return: Zero value to succesful
 */
int main(void)

	char ch;

	for (ch = 'z';ch >= 'a'; ch--)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
