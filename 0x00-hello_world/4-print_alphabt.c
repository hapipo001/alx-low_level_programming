#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 * followed by a new line, except q and e
 * Return: Always 0 (Sucess)
 */
int main(void)
{
char ch = 'a';

while (ch <= 'z')
{
putchar(ch);
}
ch++;
}
putcha('\n');
return (0);
}
